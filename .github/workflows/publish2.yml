name: Build and Release JAR2

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Build JAR
        run: ./gradlew clean build

      - name: Upload JAR artifact
        uses: actions/upload-artifact@v3
        with:
          name: app-jar
          path: build/libs/exposd-all.jar # مسیر دقیق فایل JAR

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Download JAR artifact
        uses: actions/download-artifact@v3
        with:
          name: app-jar

      - name: Generate tag
        id: generate_tag
        run: echo "::set-output name=tag::v$(date +'%Y%m%d-%H%M%S')"

      - name: Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: exposd-all.jar # اشاره به فایل JAR دانلود شده
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ steps.generate_tag.outputs.tag }}
          name: "Release ${{ steps.generate_tag.outputs.tag }}"
          body: |
            This release contains the latest build of the project.
            Download the JAR file below.
          draft: false
          prerelease: false

      - name: install cowsay
        run: |
          sudo apt-get update
          sudo apt-get install cowsay
          cowsay "release is done: ${{ steps.generate_tag.outputs.tag }}"

      - run: |
          echo "release is done: ${{ steps.generate_tag.outputs.tag }}" > release.txt
          git config --local user.email "
      - run: |
          echo "release is done: ${{ steps.generate_tag.outputs.tag }}" >> $GITHUB_STEP_FILE
          echo "release is done: ${{ steps.generate_tag.outputs.tag }}" >> $GITHUB_ENV
          echo "release is done: ${{ steps.generate_tag.outputs.tag }}" >> $GITHUB_PATH
          echo "release is done: ${{ steps.generate_tag.outputs.tag }}" >> $GITHUB_WORKSPACE
#          echo "release is done: ${{ steps.generate_tag.outputs.tag }}" >> $GITHUB_WORKSPACE/release.txt
          echo "release is done: ${{ steps.generate_tag.outputs.tag }}" >> $GITHUB_WORKSPACE/$GITHUB_STEP_FILE
          echo "release is done: ${{ steps.generate_tag.outputs.tag }}" >> $GITHUB_WORKSPACE/$GITHUB_ENV
          echo "release is done: ${{ steps.generate_tag.outputs.tag }}" >> $GITHUB_STEP_SUMMARY
